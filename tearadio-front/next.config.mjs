/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['pbs.twimg.com'],
    },
    // Diğer yapılandırmalarınız buraya eklenebilir
  };
  
  export default nextConfig;
  